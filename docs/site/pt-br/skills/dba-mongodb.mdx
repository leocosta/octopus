---
title: dba-mongodb
description: "MongoDB heuristics catalogue for the dba role. Detects indexing (ESR rule), modeling (embed vs reference), query, migration, retention (TTL), capacity (shard key), security, and concurrency problems. Every finding ships with a Mongo shell command."
draft: true
---

{/* TODO: Introduction — the hook (1–2 sentences) */}

## What it solves

{/* TODO: the concrete pain this skill removes */}

## How it works

{/* TODO: the mechanism */}

## Usage

{/* TODO: invocation + parameters (flag → what it does → default) */}
