---
title: compress-skill
description: Shrink a SKILL.md without changing its meaning. Deterministic cleanup pass first, LLM rewrite pass only if the target is not met. Anchors extracted from the skill's test file are preserved; frontmatter, headings, and code blocks are never modified. Dry-run by default; `--apply` writes the result.
draft: true
---

<!-- TODO: Introduction — the hook (1–2 sentences) -->

## What it solves

<!-- TODO: the concrete pain this skill removes -->

## How it works

<!-- TODO: the mechanism -->

## Usage

<!-- TODO: invocation + parameters (flag → what it does → default) -->
