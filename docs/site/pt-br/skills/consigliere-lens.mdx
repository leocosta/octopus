---
title: consigliere-lens
description: Make the generic knowledge engines (hygiene/synthesize/briefing) read like the consigliere when run against the private manager-workspace: political risk surfaced, the per-node playbook.md heuristics applied, the "thinks like you" voice. A wrapper — the engines stay generic; the deterministic `octopus lens` helper surfaces the grounded material; the consigliere role (opus) frames it. Read-only over the workspace (ADR-007).
draft: true
---

<!-- TODO: Introduction — the hook (1–2 sentences) -->

## What it solves

<!-- TODO: the concrete pain this skill removes -->

## How it works

<!-- TODO: the mechanism -->

## Usage

<!-- TODO: invocation + parameters (flag → what it does → default) -->
