---
title: respond-to-review
description: "The Octopus PR-feedback discipline — verify the critique, ask for evidence on generic comments, separate reasoned feedback from preference, never make performative changes, ask for clarification on ambiguity. After the fixes are applied, runs the post-fix loop (commit, conditional push, hybrid inline replies, resolve threads) in a single confirmation turn — the skill is end-to-end and does not require a separate /octopus:pr-comments invocation. Active by default on every PR feedback loop."
draft: true
---

{/* TODO: Introduction — the hook (1–2 sentences) */}

## What it solves

{/* TODO: the concrete pain this skill removes */}

## How it works

{/* TODO: the mechanism */}

## Usage

{/* TODO: invocation + parameters (flag → what it does → default) */}
