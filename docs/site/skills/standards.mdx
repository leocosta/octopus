---
title: standards
description: Self-serve lookup that answers "what's our standard for X, and why?" from the team's own sources — docs/adr/ (decisions), rules/ (coding rules incl. *.local.md overrides), CONTEXT.md (vocabulary), and knowledge/ (confirmed facts) — in that precedence, with the source cited and the rationale. Never invents an answer: when no source covers the topic it says so and routes to authoring (doc-adr or a rules override). Read-only, never gates. Pairs with audit-grounding (which flags drift in a diff using the same sources) and doc-adr (which authors the missing standard). Registered in the docs bundle.
draft: true
---

<!-- TODO: Introduction — the hook (1–2 sentences) -->

## What it solves

<!-- TODO: the concrete pain this skill removes -->

## How it works

<!-- TODO: the mechanism -->

## Usage

<!-- TODO: invocation + parameters (flag → what it does → default) -->
