name: fullstack
description: Full-stack monorepos — backend + frontend patterns, component/e2e tests, the dba reviewer role, and cross-stack contract drift detection. Per-engine DB heuristics ship as db-* profiles (auto-selected from the repo's driver).
category: intent
persona_question: "Does your repo contain both a backend API and a separate frontend (monorepo or coupled stacks)?"
persona_default: false
skills:
  - backend-patterns
  - test-e2e
  - frontend-patterns
  - test-component
  - audit-contracts
roles:
  - backend-developer
  - dba
  - frontend-developer
rules: []
mcp: []
hooks: null
