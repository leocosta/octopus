# Knowledge Index

<!--
  Routing table for continuous learning system.
  Each entry maps a domain to its knowledge folder.
  Agents: consult this file first to route to the correct domain.

  To add a new domain:
  1. Create /knowledge/<domain>/
  2. Create knowledge.md, hypotheses.md, rules.md inside it
  3. Add an entry below following the table format
-->

## Domain Map

| Domain | Path | Status |
|---|---|---|
| <!-- Add domains here --> | `knowledge/<domain>/` | Active |

## How to Use

1. **Before a task**: Read this index, then load the relevant domain's `rules.md` and `hypotheses.md`
2. **During a task**: If a hypothesis is being tested, note the outcome
3. **After a task**: Update `knowledge.md` with new facts, update `hypotheses.md` if any observation needs validation
